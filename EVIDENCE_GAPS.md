# EVIDENCE_GAPS.md

Issues found during audit. Nothing below has been fixed — report only, per instructions. (Two bundle label problems were already corrected in a prior frozen revision pass and are recorded here as provenance, not reopened.)

## 1. Manuscript claims whose evidence was not located

None. Every empirical claim in `Paper_1_Draft_v3.md` traces to at least one evidence file (see CLAIM_EVIDENCE_MAP.md, all 25 claims SUPPORTED).

## 2. Claims with only partial evidence

- **Streaming floor at N ≥ 16k (C007):** `exp9_longlength.csv` uses n=10 (N≤65536), n=3 (N=262144), n=2 (N=1M). The far-end floor rests on 2–3 trials per N. Manuscript does not state this n explicitly in the Length paragraph.
- **Decayed magnitudes (C013):** `exp3_decay.csv` holds a single measurement per gamma (no dispersion).
- **Chunk-norm cap (C018):** single N (4096) tested.
- **Survival uniformity (C020):** single (m=8, d=16) configuration.
- **t-test inputs:** tests run on group summary statistics (normal approximation), not raw trial pools; stored p column reads 0.0 for the three significant tests (manuscript recomputes/reports bounds instead).

## 3. Results where metadata is missing

- Most aggregate CSVs lack a `seed` column (seed determinable only where `_trials.csv` companions or config protocol records exist: base seed 42 per `config.json` / `summary.json`).
- `exp8_mixed.csv`, `exp12_bf16.csv`, `exp3_shrinking.csv`: probe logs with no trial structure (single runs by construction).
- `exp12_CIs.csv`, `exp12_ttests.csv`: no seed columns (computed summaries).
- `exp12_oom.csv`: analysis record (ok / skipped-by-analysis), not a machine crash log.

## 4. Experiments where trial counts cannot be verified

- `exp4_softmax_baseline.csv`: `trials` column reads 30 while the manuscript cites n=10 for the softmax reference. Consistent with 10 trials × 3 positions, but the file itself does not document this decomposition. Minor ambiguity only.

## 5. Figures whose underlying data cannot be located

None. All 15 embedded measured figures trace to the CSVs in EVIDENCE_INDEX §H; the 3 schematics are generated by `gen_schematics.py` and contain no measured data (captions state this).

## 6. Tables whose source data cannot be located

None. Every Table 1/2 cell traces to a CSV, a derivation, a cited specification, or an explicitly tagged open item (see CLAIM_EVIDENCE_MAP.md).

## 7. Statistical claims whose statistical source cannot be located

None. All 9 t-tests and the 193-row CI table are in `exp12_ttests.csv` / `exp12_CIs.csv`; per-trial inputs exist for all batch-2/3 probes and re-logged Exps 1–4.

## 8. Apparent contradictions between manuscript text and evidence

- **`summary.json` vs CSVs on compressibility:** `summary.json: batch3_compress_by_alphabet` records {8: 0.75, 64: 0.625, −1: 0.625}, but `exp11_compress.csv` (+ trials) records 0.675 / 0.55 / 0.65. The manuscript follows the CSVs. Claude must treat the CSVs as authoritative and must NOT quote 0.75/0.625.
- **`summary.json` vs `exp8_selective.csv` on oracle latch:** `followup_selective_oracle: 0.25` (older followup run) vs 0.40 in `exp8_selective.csv` (n=20 top-up). Manuscript follows the latter. Do not mix the two runs.
- **`claude_paper_brief.md` staleness:** the prior handoff brief contains superseded numbers (e.g. 0.75/0.625 compress, "m*≈2d" without full conditions). It is supporting material, not evidence; CSVs override it everywhere.

## 9. Experiments referenced by the manuscript that appear absent

None. (The d=128/m=4 point was absent from the original bundle and was supplied by a documented rerun now stored in `exp10_bigd.csv`; see provenance note below.)

## 10. Evidence files that appear unused by the manuscript

- `public_evidence/exp11_noisy.csv` + `exp11_noisy_trials.csv` (query-noise robustness, σ ∈ {0,0.1,0.3,1.0}): no matching claim in the manuscript. Usable or omittable at Claude's discretion; do not invent a manuscript claim for it without author approval.
- `public_evidence/pytest_report.txt`: unit-test record, correctly unused by the manuscript.
- Superseded figure PNGs (~30 alternates in `public_evidence/figures/`): correctly unused by the PDF build.

## Provenance notes (already-corrected items, recorded — do NOT re-apply)

- `exp10_bigd.csv` row (m=4, d=128, n=25, seed 42, linear_mean 0.86) is a later rerun with `experiments/associative_recall.py`, not part of the original bundle generation. It reproduces the manuscript's 0.86 exactly.
- `exp5_gated_decay.csv` row 4 γ label was corrected 1.0 → 0.9999 (horizons 10000/166355 identify it; sweep signature includes 0.9999).
